name: rrb-ntpc-graduate-job

on:
  schedule:
    - cron: '30 23 * * *'
  workflow_dispatch:

jobs:
  php-test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: run ntpc graduate check script
        run: |
          php ntpc-06-2025.php
